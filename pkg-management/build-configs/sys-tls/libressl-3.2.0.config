# > 1
./configure        \
 --build=$CBUILD   \
 --host=$CHOST     \
 --prefix=/usr     \
 --sysconfdir=/etc \
 --localstatedir=/var

# > 2
make
make install

# > 3
cd /etc/ssl
a2 https://curl.haxx.se/ca/cacert.pem
mv -vf cacert.pem cert.pem
