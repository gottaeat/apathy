# > 1
cd wpa_supplicant
cp -vf config .config

# > 2
patch -p1 < CVE-2019-16275.patch
patch -p1 < eloop.patch

# > 3
make BINDIR=/usr/bin LIBDIR=/usr/lib

# > 4
make BINDIR=/usr/bin LIBDIR=/usr/lib install
